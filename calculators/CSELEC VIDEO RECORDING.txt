CSELEC VIDEO RECORDING
2 NEW FUNCTIONS


Locator + Replace Values
Strip



----------1------------
Correcting/Replacing wrong/typo'ed inputs.

#1: Verification of Data

print("what we want to change:n\", dataframename.loc[indexnumber]) //Prints the index that we want to change.

#2: Replacing the incorrect data.

emp.loc[3, 'city'] = "New Delhi" //Sets the value of the column city in the 3rd index to "New Delhi"

#3: Print the edited index, or just print the whole dataframe to view the changes after editing. 

emp

or

emp.loc[3] //prints the 3rd index data.


---------2--------------
strip()

//String.strip(), removes leading and trailing white spaces.
str.strip()


emp['City'].str.strip('-') //Removes all '-' from City inputs.


//if gusto niyo mag strip ng specific side lang, and not lahat ng '-' ang matatanggal.

//l for left, r for right.
emp['City'].str.lstrip('-')
emp['City'].str.rstrip('-')




